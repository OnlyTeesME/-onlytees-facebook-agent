export default function Home() {
  return (
    <main style={{ maxWidth: 760, margin: "72px auto", padding: 24 }}>
      <h1>Only Tee's.ME Facebook AI Agent</h1>
      <p>The service is deployed. Use <code>/api/health</code> to verify configuration and <code>/api/webhook</code> as the Meta webhook URL.</p>
      <p>This page intentionally does not display credentials or customer data.</p>
    </main>
  );
}
